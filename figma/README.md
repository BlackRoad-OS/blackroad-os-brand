# Figma Assets

- `blackroad.fig`: export of the core brand library (logotypes, palette swatches, grid components).
- `hooks.ts`: sample REST client for fetching components or published styles from the Figma API.

Update the `.fig` file whenever new components ship. Publish the file and set `FIGMA_TOKEN` in CI to keep downstream previews current.
