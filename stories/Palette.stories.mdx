import { Meta, Canvas, Story, ArgsTable } from '@storybook/blocks';
import { ColorSwatch } from '../src/components/ColorSwatch';
import { GradientBlock } from '../src/components/GradientBlock';
import { tokens } from '../src/tokens';

<Meta title="Palette" />

# Palette

Primary neon hues, gradient energy, and grays define the BlackRoad OS field kit. Each value below maps 1:1 with the generated CSS custom properties.

## Core Colors

<Canvas>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(240px, 1fr))', gap: '16px' }}>
    <ColorSwatch name="Primary" value={`hsl(${tokens.color.primary})`} description="Use for CTAs and signal moments." />
    <ColorSwatch name="Secondary" value={`hsl(${tokens.color.secondary})`} description="Pair with primary for depth." />
    <ColorSwatch name="Accent" value={`hsl(${tokens.color.accent})`} description="Solar flare for focus rings." />
    <ColorSwatch name="Positive" value={`hsl(${tokens.color.positive})`} />
    <ColorSwatch name="Negative" value={`hsl(${tokens.color.negative})`} />
  </div>
</Canvas>

## Grays

<Canvas>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))', gap: '16px' }}>
    {Object.entries(tokens.color.neutral).map(([step, value]) => (
      <ColorSwatch key={step} name={`Neutral ${step}`} value={`hsl(${value})`} />
    ))}
  </div>
</Canvas>

## Gradients

<Canvas>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))', gap: '16px' }}>
    {Object.entries(tokens.color.gradient).map(([name, gradient]) => (
      <GradientBlock key={name} name={name} gradient={gradient} />
    ))}
  </div>
</Canvas>

## Token Args

<ArgsTable of={ColorSwatch} />
