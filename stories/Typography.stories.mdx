import { Meta, Canvas } from '@storybook/blocks';
import { TypographyPreview } from '../src/components/TypographyPreview';
import { tokens } from '../src/tokens';

<Meta title="Typography" />

# Typography

Inter drives UI legibility while JetBrains Mono underpins system readouts. Line heights and weights stay tight to keep the neon UI crisp.

<Canvas>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: '16px' }}>
    <TypographyPreview
      label="Display"
      fontFamily={tokens.typography.font.sans}
      size={tokens.typography.size["3xl"]}
      weight={tokens.typography.weight.semibold}
      lineHeight={tokens.typography.lineHeight.tight}
    />
    <TypographyPreview
      label="Title"
      fontFamily={tokens.typography.font.sans}
      size={tokens.typography.size["2xl"]}
      weight={tokens.typography.weight.bold}
      lineHeight={tokens.typography.lineHeight.snug}
    />
    <TypographyPreview
      label="Body"
      fontFamily={tokens.typography.font.sans}
      size={tokens.typography.size.md}
      weight={tokens.typography.weight.regular}
      lineHeight={tokens.typography.lineHeight.relaxed}
      sample="Signal-rich copy stays calm and readable."
    />
    <TypographyPreview
      label="Code"
      fontFamily={tokens.typography.font.mono}
      size={tokens.typography.size.sm}
      weight={tokens.typography.weight.medium}
      lineHeight={tokens.typography.lineHeight.snug}
      sample="const pulse = 'hsl(265 86% 62%)';"
    />
  </div>
</Canvas>
